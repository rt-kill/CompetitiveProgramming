#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool isSelfCrossing(vector<int>& distance) {

        // it seems helpful that we are going in a spiral
        //  there are three types of distance arrays for which we can return 1
        //      arrays that only spiral in
        //      arrays that only spiral out
        //      arrays that initially spiral out, and then spiral in

        //  this seems tricky to prove rigorously
        //  first, we must define what it means for a list of moves to spiral
        //      spiraling out would mean from a starting point,


        return 0;
    }
};
