#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    string line;
    while (true) {
        getline(std::cin, line);
        if (line.empty()) break;
    }
}

int main() {
    int t=1;
    /* cin >> t; */
    for (int i=0;i<t;i++)
        solve();

    return 0;
}
